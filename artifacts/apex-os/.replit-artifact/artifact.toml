kind = "web"
previewPath = "/"
title = "APEX OS"
version = "1.0.0"
id = "artifacts/apex-os"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24672

[services.development]
run = "pnpm --filter @workspace/apex-os run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/apex-os", "run", "build" ]
publicDir = "artifacts/apex-os/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24672"
BASE_PATH = "/"
